# Ukiwa-3 作業ルール

このリポジトリは電気主任技術者の個人サイト「ぷかぷかうきわメモ」です。

## UI
- 入力と対応する計算結果を近くに配置し、同じ結果を離れた場所に重複表示しない。
- 各方式の計算電流と、その電流から算出した機種別台数・補償電流・残留電流を同じ欄で表示する。
- 一般説明、公式の解説、注意事項は必要時に開ける折りたたみを基本とする。条件不足・定格超過は該当結果のそばに表示する。
- 海と浮き輪の現在のホームページデザインを尊重し、依頼なく全体を作り替えない。
- PCとスマホで入力しやすく、色だけに頼らず機種名と単位を明記する。

## 電気計算
- 定格・計算根拠は対象型番のメーカー一次資料で確認し、出典と適用条件を記載する。
- 耐圧トランスとリアクトルを区別し、組み合わせの電流・電圧・時間定格を考慮する。
- 残留電流を最小化する組み合わせと、許容電流を満たす最少台数を混同しない。
- 台数探索は各機種0台を含める。実機の混在接続適合を数値計算だけで保証しない。
- 公称値、公差込み値、損失未評価の範囲を区別し、表示電流と台数判定の入力を一致させる。

## 変更の確認
- 計算ロジック変更時は、代表例・境界値・0台・条件不成立・入力変更時の更新を確認する。
- 例：38mm²、100m、50Hz、10,350Vの方式Bは約312.1mA。R-1220K許容167mAで2Kのみの数値上の最少は1台。
- 例：同条件で210mの方式Bは約655.5mA。L公差込み判定では2Kのみ4台、2Kと5Kから選ぶ場合は各1台。いずれも損失未評価。
- 公開前に変更箇所をブラウザーで操作して確認する。公開済みと報告する際は実際の公開ページを確認する。
- 既存のユーザー変更を保持し、修正・検証・公開のどこまで完了したかを正確に伝える。
